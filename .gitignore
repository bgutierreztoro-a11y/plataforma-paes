# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code (config local/personal, no compartida)
.claude/settings.local.json

.vercel
.env*

# know-how interno / setup local — no debe entrar al repo público
docs/protocolo-revision-aislada.md
posthog-setup-report.md
.posthog-events.json
instrumentation-client.ts

# fuentes de análisis aisladas — defensa en profundidad (viven fuera del árbol;
# esto solo protege contra que se copien por error dentro del repo)
Material/
fuentes-analisis-aisladas/
pendiente-clasificar/
